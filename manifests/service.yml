apiVersion: v1
kind: Service
metadata:
    name: mauroconraspipelinesdotnetcoredocker
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: mauroconraspipelinesdotnetcoredocker